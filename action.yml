name: 'Hello? Yes this is he. Oh, how are you?'
branding: 
  color: 'blue'
  icon: 'phone-call'
description: 'Greet somone and record the time'
inputs:
  who-to-greet: # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you' 
runs:
  using: 'node12'
  main: 'dist/index.js'
